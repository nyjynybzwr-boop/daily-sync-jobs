name: Home smooth swipe smoke once

on:
  push:
    paths:
      - '.github/workflows/home-smooth-swipe-smoke-once.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout private source
        env:
          SOURCE_REPO: ${{ secrets.SOURCE_REPO }}
          SYNC_TOKEN: ${{ secrets.SYNC_TOKEN }}
        run: git clone --quiet --branch main --single-branch "https://x-access-token:${SYNC_TOKEN}@github.com/${SOURCE_REPO}.git" work

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Check smooth swipe UI
        working-directory: work
        run: |
          node --check web/home-ui-navigation.js
          grep -q "touchmove" web/home-ui-navigation.js
          grep -q "home-view-dragging" web/home-ui-navigation.js
          grep -q "SWIPE_COMMIT_RATIO" web/home-ui-navigation.js
          grep -q "let finished = false" web/home-ui-navigation.js
          node scripts/check-web-syntax.mjs
