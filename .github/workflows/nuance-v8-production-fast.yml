name: Nuance v8 production fast cleanup

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/nuance-v8-production-fast.yml'

concurrency:
  group: nuance-v8-production-fast
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Obsolete monolithic build replaced by sharded final production workflow.'
