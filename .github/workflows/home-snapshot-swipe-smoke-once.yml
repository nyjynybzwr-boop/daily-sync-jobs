name: Home snapshot swipe smoke once

on:
  push:
    paths:
      - '.github/workflows/home-snapshot-swipe-smoke-once.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout private source
        env:
          SOURCE_REPO: ${{ secrets.SOURCE_REPO }}
          SYNC_TOKEN: ${{ secrets.SYNC_TOKEN }}
        run: git clone --quiet --branch main --single-branch "https://x-access-token:${SYNC_TOKEN}@github.com/${SOURCE_REPO}.git" work

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Check snapshot swipe UI
        working-directory: work
        run: |
          node --check web/home-ui-navigation.js
          grep -q "startViewTransition" web/home-ui-navigation.js
          grep -q "homeSnapshotNewLeft" web/home-ui-navigation.js
          grep -q "stopImmediatePropagation" web/home-ui-navigation.js
          node scripts/check-web-syntax.mjs
