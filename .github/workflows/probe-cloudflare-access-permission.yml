name: Probe Cloudflare Access Permission

# Retired one-shot diagnostic. Kept manual-only for auditability; do not schedule.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Retired. 2026-09-05 findings: Access app READ=200, WRITE=403; identity-provider READ=200 with zero providers."
